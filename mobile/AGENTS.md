# Expo version

This project is pinned to Expo ~54.0.36 (see `package.json`). Read the exact versioned
docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code — do not assume
API shapes from a different major version.
